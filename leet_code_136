class Solution:
    def singleNumber(self, nums: List[int]) -> int:

        my_map = {}
        for x in nums:
            if x not in my_map:
                my_map[x]=1
            else:
                my_map[x]=2


        for x in nums:
            if my_map[x]==1:
                return x
        
